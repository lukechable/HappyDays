# Bug, security and performance sweep — 12 September 2026

This release includes the requested Gmail buckets/collapsible sidebar, corrected Cliniko payment information and bank-transfer investigation filters, plus the following confirmed fixes.

| Finding | Correction | Regression evidence |
| --- | --- | --- |
| Public settings queries enumerated internal values, including guest signing material and OAuth state; public mutation accepted internal keys | Explicit preference allowlist on reads/writes; validation; indexed reads. Guest signing keys rotate to version 2 when first used, with atomic installation | Authenticated/anonymous access tests, concurrent key installation |
| Guest password limits and initial key generation raced; middleware accepted unsigned guest cookies; broad `/book(.*)` pattern included staff `/bookings` | Atomic attempt reservations; cryptographic signature/issuer/audience checks; exact public route boundaries | Concurrent login tests and route smoke checks |
| Expired/viewed and cancelled signing requests still exposed document URLs and accepted state changes | Check expiration and terminal states before returning document details or processing signer actions | Expired, cancelled, declined and upload checksum/type tests |
| Redaction drew rectangles over recoverable PDF text | Rebuild redacted pages from rendered pixels in a fresh PDF; copy untouched pages. Older exports are unchanged and need re-exporting if relied on as redacted documents | Render/save/reopen test confirms no extractable text on redacted page and preserved text on untouched page |
| Online checkout trusted caller origin/time/selection and duplicate webhooks could create duplicate Cliniko records | Validate public selections, date windows, current availability, trusted origin, amount/currency/checkout identity; atomic overlapping holds and fulfillment claims; match patients by name and DOB as well as email | Public booking rejection tests, concurrent hold/webhook tests |
| Uncertain Cliniko writes could be replayed; failed automatic booking had no visible staff queue | Never automatically replay a claimed external write; show paid bookings awaiting confirmation in Payments and notify staff on caught failure | Retry guard tests. Staff must reconcile Cliniko and Stripe before creating an appointment manually |
| Mail cache keys were shared across accounts, and logout clearing finished before IndexedDB commits | Scope memory/device cache by user and connected email; reset component state on account changes; invalidate legacy device cache; await transaction completion | Cross-account message/search isolation and late-response cache tests |
| Sidebar changes could append a late pagination result into another folder; eager prefetch consumed Gmail quota | Guard stale pagination; deduplicate/persist pages; cancel queued reads; remove all-section subscriptions and bulk route/body prefetch | Smart category and Sent search tests; partial-batch failure regression |
| Gmail partial rate-limit failures silently skipped messages when advancing pages | Fail the page after exhausted retries, preserve cached UI and offer Retry; pace batch chunks | Mixed successful/limited batch test |
| EFT matcher repeatedly parsed every transaction for every invoice | Parse dates/references once per transaction and reuse a date formatter | 200 invoices × 1,000 transactions performs 1,200 date conversions, previously up to 200,200 |
| Cliniko pagination could forward Basic credentials to an arbitrary upstream link | Restrict requests to the configured Cliniko HTTPS origin and `/v1/`; reject redirects | Foreign next-link test verifies only the original request is sent |
| Email CSS could load trackers while images were blocked; text link conversion did not escape quotes | Remove image declarations through CSSOM; add restrictive iframe CSP; escape quotes | CSS tracking/position and attribute injection tests |
| CSV formula injection and Unicode download-header failures | Neutralize formula-leading cells; ASCII filename fallback plus encoded UTF-8; private/no-store download responses; reject upstream failures and empty file redemption | CSV and Unicode/header tests |
| Unbounded service-worker static cache and unrestricted notification destinations | Bound cached assets; restrict notification navigation to this app | Source/build review |
| Dependency audit reported seven advisories | PDF.js 6.2.108 (legacy build for browser compatibility), SheetJS 0.20.3, Lodash 4.18.1, Sentry browser 7.119.1. Sentry override retains the SDK APIs used by Medipass | Zero advisories after upgrade; PDF rendering test; production build |

Validation: full automated tests, TypeScript checks for frontend and Convex, ESLint, production webpack build, dependency audit, patch whitespace check, and deployed route/health checks. External financial writes, real patient bookings, outbound email and Tyro claims are not exercised during verification. The existing Railway **staging** service is the deployment target; no separate production service is configured.

Security boundaries: staff share practice data intentionally. File URLs already obtained cannot retract a downloaded copy. Existing PDF exports are not retroactively sanitized. Guest key rotation may require guest testers to sign in again. The review is an application code/dependency sweep, not an independent penetration test.

Dependency references: [PDF.js advisory](https://github.com/mozilla/pdf.js/security/advisories/GHSA-hq66-cqwq-w95j), [official SheetJS installation](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/), [Sentry advisory](https://github.com/advisories/GHSA-593m-55hh-j8gv). The SheetJS tarball is pinned in the lockfile with integrity verification because npm's `xlsx` package remains outdated.
